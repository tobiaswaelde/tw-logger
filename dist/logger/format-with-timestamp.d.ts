/**
 * Cutom formatter
 *
 * Displays a log message in the following format:
 * `[${timestamp}] ${level} ${message}`
 *
 * @returns {String} Log message with timestamp
 */
export declare const formatWithTimestamp: import("logform").Format;
